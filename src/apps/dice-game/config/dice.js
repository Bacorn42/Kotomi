const diceConfig = {
    defaultDiceCount: 8,
    defaultCooldownMs: 5000,
    defaultWeights: [60, 50, 40, 30, 20, 10],
    scoreMultiplier: 1,
    moneyMultiplier: 1,
    itemDropChance: 0.01,
};

module.exports = diceConfig;
